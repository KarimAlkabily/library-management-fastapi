#Books Module

* GET    /books        → كل الكتب
* GET    /books/{id}   → كتاب واحد
* POST   /books        → إضافة كتاب
* PUT    /books/{id}   → تعديل
* DELETE /books/{id}   → حذف

#Relations
![Database Diagram] (./Relations.png)